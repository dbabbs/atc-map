export const APP_URL = process.env.NEXT_PUBLIC_APP_URL;

export const TABLES = {
  profiles: "profiles",
  buildingApiUsage: "building_api_usage",
  layersApiUsage: "layers_api_usage",
  events: "events",
};

export const BASIC_PLAN_FEATURES = {
  numProjects: 5,
};

export const STANDARD_PLAN_FEATURES = {
  numProjects: 50,
};

export const EVENTS = {
  upgradeButtonClicked: "upgradeButtonClicked",
};

export const CALENDER_URL = `https://cal.com/useradiance`;
